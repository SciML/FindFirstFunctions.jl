var documenterSearchIndex = {"docs":
[{"category":"section","location":"strategies/#Search-strategies","page":"Search strategies","text":"The strategies form the parameter space of the sorted-search API. The entry points are searchsorted_last / searchsorted_first, and a strategy can be passed to them two ways:\n\nAs a StrategyKind enum value (e.g. KIND_BRACKET_GALLOP). One enum value per singleton strategy; runtime if/elseif dispatch into the matching kernel; ~0 ns of overhead in hot loops; the inferred return type is concrete regardless of which kind is picked at runtime. Use this when the strategy is chosen from runtime data.\nAs a singleton strategy struct (e.g. BracketGallop()). The struct forwards through strategy_kind, which constant-folds for a literal strategy argument — so for a literal strategy the struct form compiles to exactly the same code as the enum form.\n\nFindFirstFunctions does not extend Base.searchsortedlast / Base.searchsortedfirst. The v2 API that did is removed in v3; these FFF-owned functions are the only search entry points.\n\nThe stateful strategies — Auto and GuesserHint — carry per-instance data and so cannot be expressed as singleton enum tags. They dispatch via their own multimethods.","title":"Search strategies"},{"category":"section","location":"strategies/#Kind-strategy-mapping","page":"Search strategies","text":"Enum tag Strategy struct Kernel function\nKIND_BINARY_BRACKET BinaryBracket _kernel_last_binary_bracket / _kernel_first_binary_bracket\nKIND_LINEAR_SCAN LinearScan _kernel_last_linear_scan / _kernel_first_linear_scan\nKIND_SIMD_LINEAR_SCAN SIMDLinearScan _kernel_last_simd_linear_scan / _kernel_first_simd_linear_scan\nKIND_BRACKET_GALLOP BracketGallop _kernel_last_bracket_gallop / _kernel_first_bracket_gallop\nKIND_EXP_FROM_LEFT ExpFromLeft _kernel_last_exp_from_left / _kernel_first_exp_from_left\nKIND_INTERPOLATION_SEARCH InterpolationSearch _kernel_last_interpolation_search / _kernel_first_interpolation_search\nKIND_BIT_INTERPOLATION_SEARCH BitInterpolationSearch _kernel_last_bit_interpolation_search / _kernel_first_bit_interpolation_search\nKIND_UNIFORM_STEP UniformStep _kernel_last_uniform_step / _kernel_first_uniform_step\nKIND_BISECT_THEN_SIMD BisectThenSIMD (positional dispatch falls back to BinaryBracket; equality dispatch goes through findfirstsortedequal)\n\nStateful strategies that do not have an enum tag and stay on the multimethod path:\n\nAuto: carries a StrategyKind field plus a SearchProperties cache. Auto's searchsorted_last is a one-line forward to the stored kind; the batched dispatch re-resolves the kind from (v, queries) because the gap heuristic needs the queries.\nGuesserHint: carries a Guesser (with its idx_prev::Ref{Int} and linear_lookup::Bool). Dispatches via its own searchsorted_last(::GuesserHint, ...) / searchsorted_first(::GuesserHint, ...) methods.","title":"Kind ↔ strategy mapping"},{"category":"section","location":"strategies/#When-to-pick-which","page":"Search strategies","text":"For most callers the answer is: pass Auto (the default in the batched API) and let it choose. The table below is for callers who already know their access pattern and want to pin a strategy.\n\nStrategy Best when Cost when hint hits Cost worst case Uses hint?\nLinearScan answer is within a handful of slots of the hint O(1) O(n) yes\nSIMDLinearScan DenseVector{Int64} or DenseVector{Float64}, forward walk past the hint O(1) O(n/8) yes\nBracketGallop answer may be either side of the hint, distance unknown O(1) ~2 log₂ n yes\nExpFromLeft sorted batch — hint is prev_result, answer is monotonically ≥ hint O(1) O(log n) yes (as lower bound)\nInterpolationSearch v is uniformly spaced and numeric O(1) O(log n) no\nBitInterpolationSearch DenseVector{Float64} and log-spaced (geometric) — opt-in, Auto does not pick O(1) O(log n) no\nBinaryBracket no hint available, or fallback O(log n) O(log n) no\nUniformStep v isa AbstractRange (or known-uniformly-spaced) O(1) O(1) no\nGuesserHint repeated correlated lookups against the same v O(1) ~2 log₂ n self-provided\nAuto unknown access pattern varies varies yes if supplied\n\nAll hint-consuming strategies fall back to BinaryBracket when no hint is supplied or when the hint is out of range. InterpolationSearch additionally falls back to BinaryBracket for non-numeric element types.","title":"When to pick which"},{"category":"section","location":"strategies/#Migrating-from-v2-(Base.searchsortedlast(::S,-...))-to-v3","page":"Search strategies","text":"The v2 Base.searchsortedlast(::S, ...) / Base.searchsortedfirst(::S, ...) methods are removed in v3. The migration is a mechanical rename — searchsortedlast → searchsorted_last, searchsortedfirst → searchsorted_first — with the strategy argument unchanged:\n\n# v2 (removed)\nsearchsortedlast(BracketGallop(), v, x, hint)\nsearchsortedfirst(InterpolationSearch(), v, x)\nsearchsortedlast(Auto(v), v, x, hint)\nsearchsortedfirst(GuesserHint(g), v, x)\n\n# v3\nsearchsorted_last(BracketGallop(), v, x, hint)\nsearchsorted_first(InterpolationSearch(), v, x)\nsearchsorted_last(Auto(v), v, x, hint)\nsearchsorted_first(GuesserHint(g), v, x)\n\nHot loops that pick the strategy from runtime data should use the StrategyKind enum form (searchsorted_last(KIND_BRACKET_GALLOP, v, x, hint)); for a literal singleton strategy the struct form compiles to exactly the same code.\n\nThe batched API (searchsortedlast!, searchsortedfirst!, searchsortedrange) and the equality API (findequal, findfirstequal, findfirstsortedequal) are FFF-owned names and are unchanged.","title":"Migrating from v2 (Base.searchsortedlast(::S, ...)) to v3"},{"category":"section","location":"strategies/#Reference","page":"Search strategies","text":"","title":"Reference"},{"category":"section","location":"strategies/#Equality-search-through-the-strategy-framework","page":"Search strategies","text":"Strategies answer positional questions (\"where would x insert?\"). Equality asks a different question (\"is x at exactly which index?\"). The findequal wrapper builds the latter on top of the former: every strategy gets an equality variant for free.\n\nThe sentinel for \"not found\" is firstindex(v) - 1 (= 0 for 1-based vectors). Type-stable Int return, no Union with Nothing. Callers can test for absence with i < firstindex(v).\n\nfindequal routes most strategies through searchsorted_first + post-check generically, so findequal(BracketGallop(), v, x, hint), findequal(SIMDLinearScan(), v, x, hint), findequal(GuesserHint(g), v, x), findequal(KIND_BRACKET_GALLOP, v, x, hint), etc. all just work.\n\nThe BisectThenSIMD strategy short-circuits the post-check path on DenseVector{Int64} by dispatching into findfirstsortedequal directly — same custom LLVM IR scan, exposed through the strategy framework.\n\nGuesserHint is documented on the Guessers page.","title":"Equality search through the strategy framework"},{"category":"section","location":"strategies/#Notes-on-individual-strategies","page":"Search strategies","text":"","title":"Notes on individual strategies"},{"category":"section","location":"strategies/#LinearScan","page":"Search strategies","text":"Walks ±1 from the hint until the answer is bracketed. Cheapest possible search when the hint is right next to the answer — two comparisons. The only strategy whose worst case is O(n), so it should only be picked when the caller has strong evidence that the hint is close.\n\nFor length(v) ≤ 16, LinearScan is faster than BracketGallop even from a bad hint because the bracket bookkeeping costs more than a worst-case walk across a vector that short. Auto's resolution rule picks KIND_LINEAR_SCAN below that threshold.","title":"LinearScan"},{"category":"section","location":"strategies/#SIMDLinearScan","page":"Search strategies","text":"Same algorithm as LinearScan, with the forward walk past the hint lowered to 8-wide SIMD chunks via custom LLVM IR. The backward walk (when the hint is past the answer) uses the scalar LinearScan path — the SIMD primitive is only defined in the forward direction.\n\nSpecialized for DenseVector{Int64} and DenseVector{Float64}. Any other element type falls back to the scalar LinearScan walk. The dispatch is static — there's no runtime type test on a hot path.\n\nCaveats:\n\nElement types: Int64 and Float64 only.\nNaN: a NaN element in a Float64 vector compares as false — the SIMD scan silently skips it. Sorted Float64 vectors containing NaN aren't well-defined under any total order.\nOrder: Forward and Reverse only.\nNo hint: falls back to BinaryBracket.\nAuto does not pick this strategy by default in the per-query path. The batched dispatch picks it inside a gap window where the SIMD chunk pays for itself.","title":"SIMDLinearScan"},{"category":"section","location":"strategies/#BitInterpolationSearch","page":"Search strategies","text":"InterpolationSearch with the extrapolation guess computed on the IEEE bit pattern of v rather than the float values themselves. Wins on log-spaced (geometric) data — sometimes O(1) versus O(log n) refinement cost.\n\nOpt-in only. Auto does not pick BitInterpolationSearch.\n\nFalls back to plain InterpolationSearch on non-Float64 dense eltypes, and to BinaryBracket for non-positive or non-finite Float64 data.","title":"BitInterpolationSearch"},{"category":"section","location":"strategies/#BracketGallop","page":"Search strategies","text":"Galloping search around the hint: expand [lo, hi] outward by doubling steps until x is bracketed, then binary-search inside [lo, hi]. Direction is inferred from v[hint] vs. x, so the hint can be either above or below the answer. Worst case is ~2 log₂ n — about twice plain binary search — and it matches O(1) when the hint is close.\n\nThis is the workhorse hinted strategy and the natural choice for get_idx-style callers where the hint is a cached previous result.","title":"BracketGallop"},{"category":"section","location":"strategies/#ExpFromLeft","page":"Search strategies","text":"Exponential search forward from a hint interpreted as a lower bound. The algorithm probes v[lo], v[lo+1], …, v[lo+4] linearly, then v[lo+8], v[lo+16], … exponentially, then binary-searches inside the final bracket.\n\nUsed by Auto's batched dispatch when the queries are sorted: each call passes hint = previous_result, which by sortedness satisfies the \"answer ≥ hint\" precondition.","title":"ExpFromLeft"},{"category":"section","location":"strategies/#InterpolationSearch","page":"Search strategies","text":"Computes a guess via linear extrapolation between v[lo] and v[hi], then refines with a bounded binary search around that guess. On uniformly-spaced numeric data the first guess is the right answer — O(1) per query independent of n.\n\nTwo restrictions:\n\nNumeric eltype: non-numeric eltypes fall back to BinaryBracket.\nForward ordering only: non-Forward orderings fall back to BinaryBracket.\n\nThe hint is ignored — the guess is computed fresh from the endpoints.","title":"InterpolationSearch"},{"category":"section","location":"strategies/#BinaryBracket","page":"Search strategies","text":"Plain Base.searchsortedlast / Base.searchsortedfirst. Provided as a strategy so callers can opt out of hint-based behaviour explicitly, and so other strategies have a well-defined name to fall back to. Ignores any hint.","title":"BinaryBracket"},{"category":"section","location":"strategies/#Auto","page":"Search strategies","text":"See Auto: heuristics and benchmarks for the full decision tree and the benchmark sweep that produced its crossover constants.\n\nIn v3, Auto carries a stored StrategyKind plus a SearchProperties cache:\n\nAuto() defaults to KIND_BINARY_BRACKET. Safe but no faster than plain Base.searchsortedlast.\nAuto(v) resolves the kind from length(v) and SearchProperties(v). Picks KIND_UNIFORM_STEP for AbstractRange / detected-uniform vectors, KIND_LINEAR_SCAN for short vectors, KIND_BRACKET_GALLOP otherwise.\nAuto(v, props) is the same with a pre-computed props cache.\n\nThe per-query searchsorted_last(::Auto, v, x, hint) is a one-line forward to searchsorted_last(s.kind, v, x, hint). The batched searchsortedlast!(out, v, queries; strategy = Auto()) re-resolves the kind from (v, queries) to consult the gap heuristic.","title":"Auto"},{"category":"section","location":"strategies/#Equality-routines","page":"Search strategies","text":"The package exposes two Union{Int, Nothing}-returning equality routines — findfirstequal (unsorted SIMD scan) and findfirstsortedequal (sorted bisect-then-SIMD scan). See the Equality search page for the full documentation.","title":"Equality routines"},{"category":"type","location":"strategies/#FindFirstFunctions.SearchStrategy","page":"Search strategies","text":"SearchStrategy\n\nAbstract supertype for sorted-search strategies. Two flavours of concrete subtype:\n\nSingleton strategies (LinearScan, SIMDLinearScan, BracketGallop, ExpFromLeft, InterpolationSearch, BitInterpolationSearch, BinaryBracket, UniformStep, BisectThenSIMD) are zero-field structs. Each one has a matching StrategyKind enum value; pass either the struct or its enum tag to searchsorted_last / searchsorted_first. The struct form forwards through strategy_kind and constant-folds for a literal strategy, so it compiles to the same code as the enum tag.\nStateful strategies (Auto, GuesserHint) carry per-instance data. They dispatch via their own searchsorted_last / searchsorted_first multimethods.\n\nStrategies can also be passed to the batched searchsortedlast! / searchsortedfirst! APIs.\n\n\n\n\n\n","title":"FindFirstFunctions.SearchStrategy"},{"category":"type","location":"strategies/#FindFirstFunctions.StrategyKind","page":"Search strategies","text":"StrategyKind\n\nEnum tag identifying a singleton search strategy. Use values of this enum as the first positional argument to searchsorted_last and searchsorted_first:\n\nsearchsorted_last(KIND_BRACKET_GALLOP, v, x, hint)\n\nEach tag corresponds to one of the singleton strategy types (e.g. KIND_BRACKET_GALLOP ↔ BracketGallop). Stateful strategies (GuesserHint) do not have an enum tag — they dispatch through their wrapper struct directly.\n\nThe enum is stored as UInt8 so passing it through to dispatcher functions costs the same as a Bool and does not enlarge any struct that carries it.\n\n\n\n\n\n","title":"FindFirstFunctions.StrategyKind"},{"category":"function","location":"strategies/#FindFirstFunctions.searchsorted_last","page":"Search strategies","text":"searchsorted_last(kind::StrategyKind, v, x[, hint]; order = Base.Order.Forward)\nsearchsorted_last(s, v, x[, hint]; order = Base.Order.Forward)\n\nFFF-owned positional search for the largest index i with v[i] ≤ x under order (or v[i] ≥ x under Base.Order.Reverse). The polarity matches Base.searchsortedlast.\n\nv must be sorted in ascending order under order. Like Base.searchsortedlast, the precondition is assumed, not checked — the result on unsorted v is undefined.\n\nWhen the first argument is a StrategyKind value the call dispatches via a runtime if/elseif branch on the enum into the matching kernel. When the first argument is a stateful strategy wrapper (Auto, GuesserHint) the call dispatches via multimethod into that wrapper's own searchsorted_last method.\n\nThis is the only search entry point: as of v3, FindFirstFunctions no longer extends Base.searchsortedlast / Base.searchsortedfirst with strategy methods.\n\n\n\n\n\n","title":"FindFirstFunctions.searchsorted_last"},{"category":"function","location":"strategies/#FindFirstFunctions.searchsorted_first","page":"Search strategies","text":"searchsorted_first(kind::StrategyKind, v, x[, hint]; order = Base.Order.Forward)\nsearchsorted_first(s, v, x[, hint]; order = Base.Order.Forward)\n\nFFF-owned positional search for the smallest index i with v[i] ≥ x under order (or v[i] ≤ x under Base.Order.Reverse). As with searchsorted_last, v must be sorted in ascending order under order (assumed, not checked). See searchsorted_last for the dispatch story.\n\n\n\n\n\n","title":"FindFirstFunctions.searchsorted_first"},{"category":"function","location":"strategies/#FindFirstFunctions.strategy_kind","page":"Search strategies","text":"strategy_kind(s::SearchStrategy) -> StrategyKind\n\nMap a singleton strategy struct to its enum tag. Auto returns its stored resolved kind. GuesserHint (genuinely stateful, no singleton tag) throws ArgumentError.\n\n\n\n\n\n","title":"FindFirstFunctions.strategy_kind"},{"category":"constant","location":"strategies/#FindFirstFunctions.KIND_BINARY_BRACKET","page":"Search strategies","text":"KIND_BINARY_BRACKET::StrategyKind\n\nSelect BinaryBracket, the plain binary-search fallback for positional sorted searches.\n\n\n\n\n\n","title":"FindFirstFunctions.KIND_BINARY_BRACKET"},{"category":"constant","location":"strategies/#FindFirstFunctions.KIND_LINEAR_SCAN","page":"Search strategies","text":"KIND_LINEAR_SCAN::StrategyKind\n\nSelect LinearScan, the hinted strategy that walks linearly from a nearby index.\n\n\n\n\n\n","title":"FindFirstFunctions.KIND_LINEAR_SCAN"},{"category":"constant","location":"strategies/#FindFirstFunctions.KIND_SIMD_LINEAR_SCAN","page":"Search strategies","text":"KIND_SIMD_LINEAR_SCAN::StrategyKind\n\nSelect SIMDLinearScan, the hinted linear scan with SIMD-specialized forward walks for supported dense vectors.\n\n\n\n\n\n","title":"FindFirstFunctions.KIND_SIMD_LINEAR_SCAN"},{"category":"constant","location":"strategies/#FindFirstFunctions.KIND_BRACKET_GALLOP","page":"Search strategies","text":"KIND_BRACKET_GALLOP::StrategyKind\n\nSelect BracketGallop, the hinted strategy that expands a bidirectional exponential bracket before binary searching inside it.\n\n\n\n\n\n","title":"FindFirstFunctions.KIND_BRACKET_GALLOP"},{"category":"constant","location":"strategies/#FindFirstFunctions.KIND_EXP_FROM_LEFT","page":"Search strategies","text":"KIND_EXP_FROM_LEFT::StrategyKind\n\nSelect ExpFromLeft, the hinted strategy that treats the hint as a left bound and searches forward exponentially.\n\n\n\n\n\n","title":"FindFirstFunctions.KIND_EXP_FROM_LEFT"},{"category":"constant","location":"strategies/#FindFirstFunctions.KIND_INTERPOLATION_SEARCH","page":"Search strategies","text":"KIND_INTERPOLATION_SEARCH::StrategyKind\n\nSelect InterpolationSearch, the strategy that guesses from linear extrapolation across the vector endpoints.\n\n\n\n\n\n","title":"FindFirstFunctions.KIND_INTERPOLATION_SEARCH"},{"category":"constant","location":"strategies/#FindFirstFunctions.KIND_BIT_INTERPOLATION_SEARCH","page":"Search strategies","text":"KIND_BIT_INTERPOLATION_SEARCH::StrategyKind\n\nSelect BitInterpolationSearch, the opt-in interpolation strategy that guesses from Float64 bit patterns.\n\n\n\n\n\n","title":"FindFirstFunctions.KIND_BIT_INTERPOLATION_SEARCH"},{"category":"constant","location":"strategies/#FindFirstFunctions.KIND_UNIFORM_STEP","page":"Search strategies","text":"KIND_UNIFORM_STEP::StrategyKind\n\nSelect UniformStep, the direct arithmetic lookup strategy for uniformly-spaced vectors and ranges.\n\n\n\n\n\n","title":"FindFirstFunctions.KIND_UNIFORM_STEP"},{"category":"constant","location":"strategies/#FindFirstFunctions.KIND_BISECT_THEN_SIMD","page":"Search strategies","text":"KIND_BISECT_THEN_SIMD::StrategyKind\n\nSelect BisectThenSIMD, the sorted-equality strategy used by findequal. The positional sorted-search dispatch falls back to BinaryBracket.\n\n\n\n\n\n","title":"FindFirstFunctions.KIND_BISECT_THEN_SIMD"},{"category":"type","location":"strategies/#FindFirstFunctions.LinearScan","page":"Search strategies","text":"LinearScan <: SearchStrategy\n\nWalk ±1 from the hint. Best when the target is within a few positions of the hint. Falls back to BinaryBracket when no hint is supplied.\n\nMaps to KIND_LINEAR_SCAN.\n\n\n\n\n\n","title":"FindFirstFunctions.LinearScan"},{"category":"type","location":"strategies/#FindFirstFunctions.SIMDLinearScan","page":"Search strategies","text":"SIMDLinearScan <: SearchStrategy\n\nVariant of LinearScan whose forward walk is lowered to 8-wide SIMD chunks via custom LLVM IR. Specialized for DenseVector{Int64} and DenseVector{Float64}; for any other element type, falls back to plain LinearScan. The backward walk (when the hint is past the answer) uses the scalar LinearScan path regardless of element type.\n\nMaps to KIND_SIMD_LINEAR_SCAN.\n\nWins on long forward walks (≥ 8 elements past the hint). For walks of 1–3 elements LinearScan is comparable — the SIMD chunk has constant setup overhead. Worst case is O(n / 8) which is still linear, so SIMDLinearScan is only Auto-relevant for small n or small-gap batches where plain LinearScan would have been picked anyway.\n\nCaveats:\n\nElement type must be exactly Int64 or Float64.\nSorted-Float64 vectors containing NaN produce undefined results.\nFalls back to BinaryBracket when no hint is supplied.\nFalls back to LinearScan for non-Forward orderings.\n\n\n\n\n\n","title":"FindFirstFunctions.SIMDLinearScan"},{"category":"type","location":"strategies/#FindFirstFunctions.BracketGallop","page":"Search strategies","text":"BracketGallop <: SearchStrategy\n\nExpand an exponential bracket bidirectionally from the hint, then binary-search inside the bracket. Effectively O(1) when the target is near the hint; never worse than ~2 log₂ n comparisons.\n\nMaps to KIND_BRACKET_GALLOP. Falls back to BinaryBracket when no hint is supplied.\n\n\n\n\n\n","title":"FindFirstFunctions.BracketGallop"},{"category":"type","location":"strategies/#FindFirstFunctions.ExpFromLeft","page":"Search strategies","text":"ExpFromLeft <: SearchStrategy\n\nExponential search forward from the hint (interpreted as a left bound), then binary search in the final bracket. Best for batched sorted queries where each next query's hint is the previous result.\n\nMaps to KIND_EXP_FROM_LEFT. Falls back to BinaryBracket when no hint is supplied.\n\n\n\n\n\n","title":"FindFirstFunctions.ExpFromLeft"},{"category":"type","location":"strategies/#FindFirstFunctions.InterpolationSearch","page":"Search strategies","text":"InterpolationSearch <: SearchStrategy\n\nGuesses an index by linearly extrapolating x between v[lo] and v[hi], then refines with a bounded binary search.\n\nMaps to KIND_INTERPOLATION_SEARCH. Ignores any hint. Falls back to BinaryBracket for non-numeric element types.\n\n\n\n\n\n","title":"FindFirstFunctions.InterpolationSearch"},{"category":"type","location":"strategies/#FindFirstFunctions.BitInterpolationSearch","page":"Search strategies","text":"BitInterpolationSearch <: SearchStrategy\n\nVariant of InterpolationSearch that reinterprets DenseVector{Float64} as DenseVector{UInt64} before computing the extrapolation guess. Wins on log-spaced (geometric) data.\n\nMaps to KIND_BIT_INTERPOLATION_SEARCH.\n\nConstraints:\n\nDenseVector{Float64} only.\nRequires v[1] > 0 and the query x > 0.\nForward / Reverse orderings only.\n\nOpt-in only — Auto does not pick this strategy. Falls back to InterpolationSearch for non-Float64 dense eltypes, and to BinaryBracket for non-positive or non-finite Float64 data.\n\n\n\n\n\n","title":"FindFirstFunctions.BitInterpolationSearch"},{"category":"type","location":"strategies/#FindFirstFunctions.BinaryBracket","page":"Search strategies","text":"BinaryBracket <: SearchStrategy\n\nPlain Base.searchsortedlast / Base.searchsortedfirst. Ignores any hint.\n\nMaps to KIND_BINARY_BRACKET.\n\n\n\n\n\n","title":"FindFirstFunctions.BinaryBracket"},{"category":"type","location":"strategies/#FindFirstFunctions.BisectThenSIMD","page":"Search strategies","text":"BisectThenSIMD <: SearchStrategy\n\nEquality-search strategy. Binary-bisects v down to a small basecase, then SIMD-scans the basecase for exact equality with x. Specialised for DenseVector{Int64} + Int64 queries.\n\nMaps to KIND_BISECT_THEN_SIMD. Meant for use with findequal, not with searchsortedfirst / searchsortedlast — in the positional API it falls back to BinaryBracket.\n\n\n\n\n\n","title":"FindFirstFunctions.BisectThenSIMD"},{"category":"type","location":"strategies/#FindFirstFunctions.UniformStep","page":"Search strategies","text":"UniformStep <: SearchStrategy\n\nO(1) direct-arithmetic lookup for uniformly-spaced vectors.\n\nMaps to KIND_UNIFORM_STEP. Specialized for AbstractRange{<:Real}; for other vector types, falls back to BinaryBracket. Ignores any hint.\n\n\n\n\n\n","title":"FindFirstFunctions.UniformStep"},{"category":"type","location":"strategies/#FindFirstFunctions.Auto","page":"Search strategies","text":"Auto <: SearchStrategy\nAuto()\nAuto(props::SearchProperties)\nAuto(v::AbstractVector)\nAuto(v::AbstractVector, props::SearchProperties)\n\nStateful strategy that resolves to a concrete StrategyKind at construction time. The resolution uses static information available at construction: props (if supplied) plus v (if supplied).\n\nPer-query (searchsorted_last(Auto(), v, x[, hint])): forwards directly to the stored kind. Auto() defaults to KIND_BINARY_BRACKET (safe choice when nothing is known about v); Auto(v) resolves to a faster kind based on length(v), props.is_uniform, etc. Callers that want the v2-era \"pick at every query based on length and hint\" behaviour should explicitly construct Auto(v) for each new v.\n\nBatched sorted (searchsortedlast!(out, v, queries; strategy = Auto())): the batched dispatcher re-resolves the kind from (v, queries) even when Auto() carries the default kind — the gap-based decision tree requires the queries, which aren't available at Auto-construction time.\n\nCached properties. Passing a populated SearchProperties via Auto(props) or Auto(v, props) short-circuits the per-call probes. The cached path is behaviour-equivalent to Auto(v) when props is up to date for v; the caller is responsible for re-computing props if v mutates.\n\nFields\n\nkind::StrategyKind — resolved kind. Use this field directly in hot loops via searchsorted_last(auto.kind, v, x, hint) to skip the auto.props field load entirely.\nprops::SearchProperties — cached properties used by the batched decision tree.\n\n\n\n\n\n","title":"FindFirstFunctions.Auto"},{"category":"type","location":"strategies/#FindFirstFunctions.SearchProperties","page":"Search strategies","text":"SearchProperties{T}\n\nCached, non-allocating facts about a sorted vector. Pass to Auto via Auto(props) to skip the per-call probes that the default Auto() runs on every batched call.\n\nDefault-constructed (SearchProperties()) is the \"no information\" sentinel: has_props is false, the other fields are unspecified and ignored by Auto. Construct via SearchProperties(v::AbstractVector) to populate the fields by running the probes once.\n\nT is the data ratio type — the type of oneunit(eltype(v)) / oneunit(eltype(v)), so e.g. SearchProperties{Float64} for Vector{Int} (because Int/Int promotes to Float64) or Vector{Float64}, SearchProperties{Float32} for Vector{Float32}. For non-Number eltypes the default is Float64 and has_props = false.\n\nCurrently consumed by Auto:\n\nis_linear — gates InterpolationSearch in batched dispatch.\nhas_nan (Float64 only) — gates SIMDLinearScan eligibility.\nis_uniform — short-circuits to UniformStep when set, with first_val and inv_step baked in for closed-form O(1) lookup.\n\nWhen is_uniform = true, first_val and inv_step hold the precomputed data needed by UniformStep's closed-form path (idx = floor((x - first_val) * inv_step) + 1). When is_uniform = false they are zero(T) and never consulted.\n\nThe is_log_linear field is populated for callers that want to manually pin BitInterpolationSearch; Auto does not consume it.\n\n\n\n\n\n","title":"FindFirstFunctions.SearchProperties"},{"category":"function","location":"strategies/#FindFirstFunctions.findequal","page":"Search strategies","text":"findequal(strategy, v, x[, hint]; order = Base.Order.Forward) -> Int\n\nReturn the index of x in sorted v if present, or the sentinel firstindex(v) - 1 if x is absent. Type-stable Int return — the sentinel matches the convention Base.searchsortedlast already uses for \"x precedes all of v\".\n\nThe strategy argument can be:\n\nA singleton strategy struct (BinaryBracket(), BracketGallop(), …) — forwards through the struct → kind mapping.\nA StrategyKind enum value (KIND_BRACKET_GALLOP, …).\nA stateful strategy (Auto, GuesserHint) — dispatched via multimethod.\n\nMost strategies are handled generically. The shortcut method on BisectThenSIMD for DenseVector{Int64} skips the searchsortedfirst path entirely.\n\nFor unsorted vectors, use findfirstequal.\n\n\n\n\n\n","title":"FindFirstFunctions.findequal"},{"category":"section","location":"guessers/#Guessers","page":"Guessers","text":"A Guesser is a small wrapper around a sorted vector that produces an integer index hint for a given query, using one of two strategies decided at construction time:\n\nLinear-extrapolation lookup, when v is approximately uniformly spaced. The guess is firstindex(v) + round((x - v[1]) / (v[end] - v[1]) * (length(v) - 1)). Cost is O(1) per call, independent of length(v).\nCached previous result, otherwise. The guesser hands back its idx_prev field. Useful for callers that issue temporally-correlated queries — the previous answer is a good seed for BracketGallop.\n\nThe Guesser does not perform the actual search. To use it as a search strategy, wrap it in GuesserHint.","title":"Guessers"},{"category":"section","location":"guessers/#Construction","page":"Guessers","text":"The threshold passed via looks_linear_threshold is the same threshold used by looks_linear (which the Guesser calls under the hood). The default 1e-2 accepts everything from exact linear ranges through evenly-spaced grids with mild jitter, but rejects log-spaced or piecewise-spaced data.","title":"Construction"},{"category":"section","location":"guessers/#Calling-a-Guesser","page":"Guessers","text":"guesser(x) returns an integer index hint, but does not update any state. The returned hint may be outside axes(v) if x is past either end of v; callers should treat the hint as advisory and clamp.\n\nv = collect(0.0:0.1:10.0)\ng = Guesser(v)\ng(3.14)   # → 32, an O(1) extrapolation guess\n\nFor irregular data the same call returns g.idx_prev[] — the last index written by a GuesserHint search (or 1 if no search has run yet).","title":"Calling a Guesser"},{"category":"section","location":"guessers/#Plugging-into-the-strategy-dispatch","page":"Guessers","text":"GuesserHint is the strategy adapter that turns a Guesser into a SearchStrategy. It:\n\nCalls guesser(x) to obtain an integer guess.\nDispatches to BracketGallop from that guess.\nWrites the resulting index back into guesser.idx_prev.\n\nPer-call cost is one guesser(x) evaluation (O(1)) plus one BracketGallop (O(1) when the guess is close) plus one idx_prev[] write.\n\nThe vector passed to searchsortedfirst / searchsortedlast must be the same object the Guesser wraps — GuesserHint asserts v === s.guesser.v to catch the obvious misuse.\n\nv = collect(0.0:0.1:10.0)\ng = Guesser(v)\nstrat = GuesserHint(g)\n\ni = searchsorted_last(strat, v, 3.14)\n@assert g.idx_prev[] == i   # the guesser caches the last result\n\nGuesserHint ignores any externally-supplied hint — the Guesser carries its own hint state, and accepting a foreign hint would defeat the cache.","title":"Plugging into the strategy dispatch"},{"category":"section","location":"guessers/#Pattern:-correlated-lookups-for-interpolation","page":"Guessers","text":"The intended use is a wrapper struct that owns both the sorted vector and a matching Guesser. Every query against the wrapper feeds through GuesserHint:\n\nstruct Interp{V}\n    v::V\n    g::Guesser{V}\nend\nInterp(v) = Interp(v, Guesser(v))\n\nfunction find_segment(itp::Interp, x)\n    return searchsorted_last(GuesserHint(itp.g), itp.v, x)\nend\n\nAfter warmup, repeated calls to find_segment with temporally correlated x cost O(1): the previous result is one slot away from the current answer, so the BracketGallop inside GuesserHint returns after a couple of comparisons.","title":"Pattern: correlated lookups for interpolation"},{"category":"section","location":"guessers/#When-not-to-use-a-Guesser","page":"Guessers","text":"One-shot queries. Construct cost is O(n) for the looks_linear probe — wasted if you only intend to search once. Use BinaryBracket or pass Auto() with no hint.\nSorted batches. The batched searchsortedlast! / searchsortedfirst! APIs already do prev_result-style hinting internally; they don't need a Guesser and they pick a faster strategy when the sweep is dense.\nStrictly random queries against irregular v. looks_linear returns false, so the Guesser degrades to returning idx_prev[], which is useless for random queries. BracketGallop from idx_prev is fine but Auto-on-a-batch does the same thing without the Guesser overhead.","title":"When not to use a Guesser"},{"category":"type","location":"guessers/#FindFirstFunctions.Guesser","page":"Guessers","text":"Guesser(v::AbstractVector; looks_linear_threshold = 1e-2)\n\nWrapper of the searched vector v which makes an informed guess for the next correlated lookup by either\n\nexploiting that v is sufficiently evenly spaced (linear-extrapolation guess), or\nusing the previous outcome (the cached idx_prev).\n\n\n\n\n\n","title":"FindFirstFunctions.Guesser"},{"category":"function","location":"guessers/#FindFirstFunctions.looks_linear","page":"Guessers","text":"looks_linear(v; threshold = 1e-2)\n\nDetermine if the abscissae v are regularly distributed.\n\n\n\n\n\n","title":"FindFirstFunctions.looks_linear"},{"category":"type","location":"guessers/#FindFirstFunctions.GuesserHint","page":"Guessers","text":"GuesserHint(guesser::Guesser) <: SearchStrategy\n\nUses a Guesser to produce an integer guess for x, then dispatches to BracketGallop from that guess. The Guesser already decides between linear-extrapolation lookup and using the previous result as a guess; this strategy plugs that logic into the strategy dispatch hierarchy.\n\nStateful strategy. GuesserHint carries the Guesser (which carries idx_prev::Ref{Int} and linear_lookup::Bool), so it cannot be reduced to a StrategyKind tag. It dispatches via its own searchsorted_last(::GuesserHint, ...) / searchsorted_first(::GuesserHint, ...) methods.\n\nUse this strategy with the per-query and batched APIs whenever you have a Guesser attached to a vector.\n\n\n\n\n\n","title":"FindFirstFunctions.GuesserHint"},{"category":"section","location":"interface/#Interface-and-extension-rules","page":"Interface and extension rules","text":"This page documents the public sorted-search API surface and the contract a custom SearchStrategy subtype must satisfy.","title":"Interface and extension rules"},{"category":"section","location":"interface/#Public-API-surface","page":"Interface and extension rules","text":"The sorted-search public API is the pair of FFF-owned generic functions dispatched on a strategy as the first positional argument:\n\nsearchsorted_first(strategy, v, x[, hint]; order = Base.Order.Forward)\nsearchsorted_last(strategy, v, x[, hint]; order = Base.Order.Forward)\n\nstrategy is a StrategyKind enum value (KIND_BRACKET_GALLOP, …), a singleton strategy struct (BracketGallop(), … — forwards through strategy_kind and constant-folds for a literal argument), or a stateful strategy (Auto, GuesserHint).\n\nThe in-place batched variants:\n\nsearchsortedfirst!(idx_out, v, queries; strategy = Auto(), order = Base.Order.Forward)\nsearchsortedlast!(idx_out, v, queries; strategy = Auto(), order = Base.Order.Forward)\n\nFindFirstFunctions does not extend Base.searchsortedfirst / Base.searchsortedlast — all strategy dispatch happens through the FFF-owned names above, which compose with the same Base.Order orderings.","title":"Public API surface"},{"category":"section","location":"interface/#Rules-of-the-interface","page":"Interface and extension rules","text":"Hint is optional and is an Integer. When supplied it must be a valid index into v (firstindex(v) ≤ hint ≤ lastindex(v)). An out-of-range hint is silently treated as \"no hint\" by every built-in strategy. A strategy is allowed to ignore the hint entirely (InterpolationSearch, BinaryBracket).\nStrategies are singletons or wrappers. LinearScan, BracketGallop, ExpFromLeft, InterpolationSearch, and BinaryBracket are zero-field singletons mapped to their StrategyKind tag by strategy_kind. Auto carries a resolved kind plus a SearchProperties payload; GuesserHint is a thin wrapper around a Guesser. New strategies should follow the same pattern: parameters that change behaviour belong on the type; parameters that change cost only should be tuned internally.\nNo mutation of v or x. A strategy never writes to the searched vector or to the query. The only state that may change across calls is hint state carried by the strategy itself (e.g. GuesserHint updates guesser.idx_prev).\nOrder is honored. Every strategy accepts an order::Base.Order.Ordering keyword and returns indices consistent with Base.searchsortedfirst / Base.searchsortedlast under that ordering. Strategies that are only efficient under Forward ordering (e.g. InterpolationSearch, ExpFromLeft) fall back to BinaryBracket for non-Forward orderings.\nAbstractRange is already O(1). Base.searchsortedfirst(r::AbstractRange, x) has a closed-form implementation. Strategies do not need range fast paths — the BinaryBracket fallback already calls into Base's range-aware method.","title":"Rules of the interface"},{"category":"section","location":"interface/#Anatomy-of-a-strategy","page":"Interface and extension rules","text":"A built-in singleton strategy consists of a StrategyKind enum value, a pair of kernel functions (_kernel_last_<name> / _kernel_first_<name> in src/kernels.jl), and branches in the four dispatch switches in src/kinds.jl (hinted/unhinted × last/first). The \"no hint\" branch of a hint-using strategy falls back to BinaryBracket; the hinted branch of a hint-ignoring strategy discards the hint.\n\nA custom strategy defined outside the package cannot add an enum value (the enum is closed), so it provides its own searchsorted_last / searchsorted_first methods instead — these are more specific than the generic SearchStrategy fallback and take precedence:\n\n# Required: the hinted form (the strategy's reason for existing).\nFindFirstFunctions.searchsorted_last(::MyStrategy, v, x, hint::Integer; order) = ...\nFindFirstFunctions.searchsorted_first(::MyStrategy, v, x, hint::Integer; order) = ...\n\n# Required: the unhinted form. Most strategies just fall back to BinaryBracket.\nFindFirstFunctions.searchsorted_last(::MyStrategy, v, x; order) =\n    FindFirstFunctions.searchsorted_last(FindFirstFunctions.KIND_BINARY_BRACKET, v, x; order)\nFindFirstFunctions.searchsorted_first(::MyStrategy, v, x; order) =\n    FindFirstFunctions.searchsorted_first(FindFirstFunctions.KIND_BINARY_BRACKET, v, x; order)\n\nIf your strategy ignores the hint, define just the unhinted form and have the hinted form delegate to it (see BinaryBracket and InterpolationSearch in the source).","title":"Anatomy of a strategy"},{"category":"section","location":"interface/#How-to-add-a-new-strategy","page":"Interface and extension rules","text":"Two steps.","title":"How to add a new strategy"},{"category":"section","location":"interface/#1.-Define-the-strategy-type","page":"Interface and extension rules","text":"Pick a name that describes the algorithm, not the use case. Make it a subtype of SearchStrategy:\n\n\"\"\"\n    MyStrategy <: FindFirstFunctions.SearchStrategy\n\nOne sentence on what it does. One sentence on when it wins. One sentence on\nwhen it falls back.\n\"\"\"\nstruct MyStrategy <: FindFirstFunctions.SearchStrategy end\n\nIf your strategy carries state (like GuesserHint), make it a parametric struct:\n\nstruct MyStrategy{S} <: FindFirstFunctions.SearchStrategy\n    state::S\nend","title":"1. Define the strategy type"},{"category":"section","location":"interface/#2.-Implement-the-dispatch-methods","page":"Interface and extension rules","text":"At minimum, the hinted forms:\n\nfunction FindFirstFunctions.searchsorted_last(\n        ::MyStrategy, v::AbstractVector, x, hint::Integer;\n        order::Base.Order.Ordering = Base.Order.Forward\n    )\n    # Algorithm body. Must return the same index that\n    # `Base.searchsortedlast(v, x, order)` would.\n    ...\nend\n\nfunction FindFirstFunctions.searchsorted_first(\n        ::MyStrategy, v::AbstractVector, x, hint::Integer;\n        order::Base.Order.Ordering = Base.Order.Forward\n    )\n    ...\nend\n\nPlus unhinted forms (typically fallbacks):\n\nFindFirstFunctions.searchsorted_last(s::MyStrategy, v::AbstractVector, x; order = Base.Order.Forward) =\n    FindFirstFunctions.searchsorted_last(FindFirstFunctions.KIND_BINARY_BRACKET, v, x; order = order)\nFindFirstFunctions.searchsorted_first(s::MyStrategy, v::AbstractVector, x; order = Base.Order.Forward) =\n    FindFirstFunctions.searchsorted_first(FindFirstFunctions.KIND_BINARY_BRACKET, v, x; order = order)\n\nWhen a strategy contributes to the package itself, add a StrategyKind enum value, the kernel pair, the four dispatch-switch branches, and a strategy_kind method instead — see src/kinds.jl and src/strategy_kind.jl.","title":"2. Implement the dispatch methods"},{"category":"section","location":"interface/#Correctness-check","page":"Interface and extension rules","text":"Every strategy must return the same answer as plain Base.searchsortedlast / Base.searchsortedfirst for every (v, x[, hint]) triple. Test with random inputs against Base:\n\nusing Test, Random\nusing FindFirstFunctions: searchsorted_last, searchsorted_first\nRandom.seed!(0)\nfor trial in 1:10_000\n    v = sort!(randn(rand(1:1000)))\n    x = randn()\n    hint = rand(1:length(v))\n    @test searchsorted_last(MyStrategy(), v, x, hint) == searchsortedlast(v, x)\n    @test searchsorted_first(MyStrategy(), v, x, hint) == searchsortedfirst(v, x)\nend","title":"Correctness check"},{"category":"section","location":"interface/#Hooking-into-Auto","page":"Interface and extension rules","text":"Auto's decision tree lives in _auto_resolve_kind (construction-time) and _auto_batched_kind (batched). It is not extensible from outside — new strategies do not register themselves with Auto automatically. If you believe Auto should pick your strategy in some regime, open an issue with benchmark numbers across the regime grid in Auto: heuristics and benchmarks.","title":"Hooking into Auto"},{"category":"function","location":"interface/#FindFirstFunctions.searchsortedfirst!","page":"Interface and extension rules","text":"searchsortedfirst!(idx_out, v, queries; strategy = Auto(), order = Base.Order.Forward,\n                    queries_sorted = nothing)\n\nIn-place batched Base.searchsortedfirst. See searchsortedlast! for behavior.\n\n\n\n\n\n","title":"FindFirstFunctions.searchsortedfirst!"},{"category":"function","location":"interface/#FindFirstFunctions.searchsortedlast!","page":"Interface and extension rules","text":"searchsortedlast!(idx_out, v, queries; strategy = Auto(), order = Base.Order.Forward,\n                   queries_sorted = nothing)\n\nIn-place batched Base.searchsortedlast. Writes one index per element of queries into idx_out (which must be the same length).\n\nIf queries is sorted under order, the previous result is used as a hint for the next query, so the total cost is O(length(v) + length(queries)) under strategy = BracketGallop().\n\nIf queries is not sorted, falls back to per-element searchsortedlast with no hint regardless of strategy.\n\nThe queries_sorted kwarg controls the runtime issorted(queries) check:\n\nnothing (default): run issorted(queries; order = order) on every call.\ntrue: skip the check and take the sorted-loop path unconditionally.\nfalse: skip the check and take the unsorted-loop path unconditionally.\n\nReturns idx_out.\n\n\n\n\n\n","title":"FindFirstFunctions.searchsortedlast!"},{"category":"function","location":"interface/#FindFirstFunctions.searchsortedrange","page":"Interface and extension rules","text":"searchsortedrange(strategy, v, lo, hi[, hint]; order = Base.Order.Forward)\n    -> UnitRange{Int}\n\nReturn the index range of all entries v[i] satisfying lo ≤ v[i] ≤ hi under order. Equivalent to searchsorted_first(strategy, v, lo[, hint]; order) :  searchsorted_last(strategy, v, hi[, hint]; order).\n\nWhen a hint is supplied it seeds the lo search directly; the hi search is seeded with max(first_idx, hint), since the upper endpoint can never precede the lower one. Strategies that ignore the hint treat the hinted form as a pass-through.\n\n\n\n\n\n","title":"FindFirstFunctions.searchsortedrange"},{"category":"section","location":"auto/#Auto:-heuristics-and-benchmarks","page":"Auto: heuristics and benchmarks","text":"Auto is the default strategy for the batched API. This page documents the decision tree it follows, the crossover constants embedded in it, and the benchmark sweep used to validate them. The numbers below are reproducible on any machine — the script at the end of the page generates the comparison grid.","title":"Auto: heuristics and benchmarks"},{"category":"section","location":"auto/#What-Auto-decides","page":"Auto: heuristics and benchmarks","text":"The decision differs between per-query and batched callers.","title":"What Auto decides"},{"category":"section","location":"auto/#Per-query:-searchsorted_last(Auto(v),-v,-x[,-hint])","page":"Auto: heuristics and benchmarks","text":"The kind is resolved once, at construction, and every per-query call forwards to it:\n\nAuto()  (no data)                            →  KIND_BINARY_BRACKET\nAuto(v) and props.is_uniform                 →  KIND_UNIFORM_STEP   # props-aware closed form\nAuto(v) and length(v) ≤ 16                   →  KIND_LINEAR_SCAN    # _AUTO_LINEAR_THRESHOLD\nAuto(v) otherwise                            →  KIND_BRACKET_GALLOP\n\nAuto never picks InterpolationSearch or ExpFromLeft in the per-query path. Both pay setup costs (endpoint reads for InterpolationSearch, 5 linear probes for ExpFromLeft) that the per-query path can't amortize.","title":"Per-query: searchsorted_last(Auto(v), v, x[, hint])"},{"category":"section","location":"auto/#Batched:-searchsortedlast!(out,-v,-queries;-strategy-Auto())","page":"Auto: heuristics and benchmarks","text":"The decision is driven by a single number: the expected average gap in v's index space between consecutive query results. For sorted numeric queries this is floor(n * span(queries) / span(v) / m); for non-numeric or pathologically-spaced data it falls back to n ÷ m.\n\nm == 0                                       →  return out unchanged\nm == 1                                       →  one direct unhinted call\nqueries unsorted                             →  unhinted Base.searchsortedlast loop\ngap ≤ 4                                      →  LinearScan\n4 < gap ≤ _auto_simd_gap_max(v)\n        AND SIMD-eligible (Int64/Float64)    →  SIMDLinearScan\ngap ≥ 8 AND n ≥ 1024 AND m ≥ 2\n        AND not skewed\n        AND linearity probe accepts          →  InterpolationSearch\ngap ≥ 16                                     →  BracketGallop\notherwise                                    →  ExpFromLeft\n\n_auto_simd_gap_max(v) is 64 for DenseVector{Int64} and DenseVector{Float64}, and 0 (never picked) for any other element type. For Float64 the SIMD path requires v to be NaN-free; if a populated SearchProperties(v) is attached to Auto, the cached has_nan flag is consulted, otherwise no-NaN is assumed (the same trust contract Base.searchsortedlast uses for sortedness of v).\n\nThe \"linearity probe accepts\" check is tiered:\n\nFor _AUTO_INTERP_MIN_GAP ≤ gap < _AUTO_INTERP_LOOSE_GAP (8 to 256): strict 0.1% sampled tolerance — only truly uniform data passes.\nFor gap ≥ _AUTO_INTERP_LOOSE_GAP (≥ 256): loose 5% tolerance — at these gap sizes, InterpolationSearch's cache benefit compensates for a worse guess, and the bounded-binary-search refinement is still O(log n). This unlocks InterpolationSearch on approximately linear data like sorted random or mildly jittered vectors at large n.\n\nThe \"skewed\" check guards InterpolationSearch from the opposite direction: if the queries are clustered into one region of their span (median query more than 20% off the midpoint of the query span), Auto falls through to BracketGallop / ExpFromLeft, because consecutive queries land in the same neighbourhood and the previous-result hint is worth more than the linear-extrapolation guess. Skew detection is gated on m ≥ 10 — for smaller m the median sampling variance overwhelms the signal.\n\nThe BracketGallop fallback at gap ≥ _AUTO_GALLOP_GAP_MIN (= 16) exists because, at large gaps, ExpFromLeft's five up-front linear probes are guaranteed to miss — the answer is much more than 5 elements past the previous-result hint. BracketGallop skips that wasted preamble and starts doubling from one position past hint.","title":"Batched: searchsortedlast!(out, v, queries; strategy = Auto())"},{"category":"section","location":"auto/#Crossover-constants","page":"Auto: heuristics and benchmarks","text":"The constants are defined at the top of src/FindFirstFunctions.jl and reproduced here so they are easy to find from the docs:\n\nConstant Value What it gates\n_AUTO_LINEAR_THRESHOLD 16 Per-query LinearScan vs BracketGallop crossover on hinted calls.\n_AUTO_BATCH_LINEAR_GAP 4 Batched LinearScan ceiling.\n_AUTO_SIMD_GAP_MAX (Int64 / Float64) 64 / 64 Maximum gap for SIMDLinearScan on dense Int64 / Float64.\n_AUTO_GALLOP_GAP_MIN 16 Above this gap, prefer BracketGallop over ExpFromLeft when InterpolationSearch isn't picked.\n_AUTO_INTERP_MIN_GAP 8 Minimum gap below which InterpolationSearch is never picked.\n_AUTO_INTERP_MIN_N 1024 Minimum length(v) below which InterpolationSearch is never picked.\n_AUTO_INTERP_MIN_M 2 Minimum length(queries); single-query batches skip the heuristic entirely.\n_AUTO_INTERP_LOOSE_GAP 256 At this gap, the linearity probe switches to the loose tolerance.\n_AUTO_LINEAR_REL_TOLERANCE 1.0e-3 Strict-tier tolerance of the sampled-linearity probe.\n_AUTO_LINEAR_LOOSE_TOLERANCE 5.0e-2 Loose-tier tolerance, used for gap ≥ _AUTO_INTERP_LOOSE_GAP.\n\nThese are not user-tunable from outside — they shipped with the version of the package documented here. Tightening or loosening them requires a PR with new benchmark numbers across the regime grid below.","title":"Crossover constants"},{"category":"section","location":"auto/#Why-each-branch-is-there","page":"Auto: heuristics and benchmarks","text":"gap ≤ 4 → LinearScan. ExpFromLeft issues 5 unconditional linear probes at the start of every call. When the gap is 0 or 1, those probes are wasted. LinearScan walks the same handful of indices with no bracketing overhead and wins by ~30%. The crossover is empirically at gap ≈ 4 on AVX2 hardware; below that, LinearScan is strictly faster.\n\nInterpolationSearch only on linear v. On uniformly-spaced numeric data, InterpolationSearch's first guess is the answer — one comparison per query, independent of n. On irregular data the guess is bad and the binary-search fallback runs over the whole vector, costing ~14× more than ExpFromLeft on log-spaced data and ~4× more on two-scale (piecewise dense+sparse) data. The 0.1% tolerance on the linearity probe is what keeps it in the linear regime where it wins.\n\nExpFromLeft is the default sparse-batched choice. Across every non-linear v pattern tested, ExpFromLeft from prev_result is within 20% of the per-cell optimum. It handles gap = 5 (3-5 linear probes, hit) just as well as gap = 10⁴ (doubling search across 14 levels) with no setup cost worth speaking of.\n\nSkew override. Even on linear v, when consecutive queries land in the same region (clustered queries), prev_result is closer to the current answer than the linear-extrapolation guess from the endpoints. ExpFromLeft wins in that regime by ~2-3×.","title":"Why each branch is there"},{"category":"section","location":"auto/#Reproducing-the-benchmarks","page":"Auto: heuristics and benchmarks","text":"The full sweep lives at bench/auto_sweep.jl with the regime grid pre-configured. Run with julia --project=bench bench/auto_sweep.jl. It evaluates every shipped strategy against every regime cell, computes the per-cell winner, and reports Auto's slack distribution against that optimum. An analysis helper at bench/analyze.jl reads the resulting bench/results.csv and prints per-strategy win-by-regime tables.\n\nThe inline script below is the minimum-viable version of the same sweep — copy into a file and execute if you want to validate the numbers without cloning the repository.\n\nusing BenchmarkTools, FindFirstFunctions, Random, Statistics\n\nconst STRATEGIES = (\n    LinearScan(), BracketGallop(), ExpFromLeft(),\n    InterpolationSearch(), BinaryBracket(),\n)\nconst STRAT_NAMES = (\"LinearScan\", \"BracketGallop\", \"ExpFromLeft\",\n                     \"InterpolationSearch\", \"BinaryBracket\")\n\n# ----- v patterns ------------------------------------------------------------\nmake_v(:uniform,   n) = collect(range(0.0, 1.0; length = n))\nmake_v(:jittered,  n) = begin\n    rng = Xoshiro(0)\n    base = collect(range(0.0, 1.0; length = n))\n    sort!(base .+ 0.01 .* (rand(rng, n) .- 0.5) ./ n)\nend\nmake_v(:logspaced, n) = collect(exp.(range(log(1.0), log(1.0e6); length = n)))\nmake_v(:twoscale,  n) = begin\n    h = n ÷ 2\n    sort!(vcat(range(0.0, 0.1; length = h),\n               range(0.1, 1.0; length = n - h)))\nend\nmake_v(:random,    n) = sort!(rand(Xoshiro(0), n))\n\n# ----- query patterns --------------------------------------------------------\nmake_q(:dense,     v, m) = collect(range(v[1], v[end]; length = m))\nmake_q(:sparse,    v, m) = begin\n    rng = Xoshiro(1)\n    sort!(rand(rng, m) .* (v[end] - v[1]) .+ v[1])\nend\nmake_q(:clustered, v, m) = begin\n    # all queries inside one segment of v\n    j = length(v) ÷ 4\n    sort!(rand(Xoshiro(2), m) .* (v[j + 1] - v[j]) .+ v[j])\nend\nmake_q(:sorted_rand, v, m) = sort!(rand(Xoshiro(3), m) .* (v[end] - v[1]) .+ v[1])\n\n# ----- bench one cell --------------------------------------------------------\nfunction bench_cell(v_kind, q_kind, n, m)\n    v = make_v(Val(v_kind), n)\n    queries = make_q(Val(q_kind), v, m)\n    out = Vector{Int}(undef, m)\n    times = Float64[]\n    for s in STRATEGIES\n        t = @belapsed FindFirstFunctions.searchsortedlast!(\n            $out, $v, $queries; strategy = $s\n        )\n        push!(times, t)\n    end\n    t_auto = @belapsed FindFirstFunctions.searchsortedlast!(\n        $out, $v, $queries; strategy = Auto()\n    )\n    return times, t_auto\nend\n\n# wrap so we can dispatch on Val(:uniform), etc.\nmake_v(::Val{K}, n) where {K} = make_v(K, n)\nmake_q(::Val{K}, v, m) where {K} = make_q(K, v, m)\n\n# ----- sweep -----------------------------------------------------------------\nfunction run_sweep()\n    ns        = (64, 256, 1024, 4096, 16384, 65536)\n    ms        = (1, 4, 16, 64, 256, 1024, 4096)\n    v_kinds   = (:uniform, :jittered, :logspaced, :twoscale, :random)\n    q_kinds   = (:dense, :sparse, :clustered, :sorted_rand)\n\n    println(\"v_kind\\tq_kind\\tn\\tm\\tAuto/best\\tbest_strat\\tAuto_pick_strat\")\n    autoslack = Float64[]\n    for v_kind in v_kinds, q_kind in q_kinds, n in ns, m in ms\n        m > n && continue\n        times, t_auto = bench_cell(v_kind, q_kind, n, m)\n        best, j = findmin(times)\n        ratio = t_auto / best\n        push!(autoslack, ratio)\n        println(string(v_kind, \"\\t\", q_kind, \"\\t\", n, \"\\t\", m,\n                       \"\\t\", round(ratio; digits = 2),\n                       \"\\t\", STRAT_NAMES[j]))\n    end\n    println()\n    println(\"Auto-vs-best ratio summary:\")\n    println(\"  median \", round(median(autoslack); digits = 3))\n    println(\"  mean   \", round(mean(autoslack);   digits = 3))\n    println(\"  p90    \", round(quantile(autoslack, 0.9); digits = 3))\n    println(\"  max    \", round(maximum(autoslack); digits = 3))\nend\n\nrun_sweep()","title":"Reproducing the benchmarks"},{"category":"section","location":"auto/#Headline-results","page":"Auto: heuristics and benchmarks","text":"The 2.0 sweep covers 1080 cells across 5 v patterns × 4 query patterns × 5 n sizes × 6 m sizes × 2 element types (Int64, Float64), measured on AVX2 hardware.\n\nMetric Auto() Auto(SearchProperties(v))\nmedian slack 1.04× 1.03×\nmean slack 1.09× 1.08×\np90 slack 1.33× 1.31×\np95 slack 1.38× 1.38×\nmax slack 2.18× 2.09×\n\nPer-cell winner distribution across the sweep:\n\nLinearScan: 47% of cells (small-gap regime, all eltypes)\nSIMDLinearScan: 25% of cells (medium-gap regime, Int64/Float64 dense)\nInterpolationSearch: 13% of cells (large-gap regime, ~linear v)\nExpFromLeft: 8% of cells (small-medium-gap regime, non-SIMD eltypes)\nBracketGallop: 8% of cells (large-gap regime, non-linear v)\n\nThe cells where Auto slack is highest are boundary cells at m = 4 where the per-cell winner is a measurement artefact (each measurement amortises over only 4 queries, so per-call setup noise dominates). The bulk of the distribution sits well below 1.5×.","title":"Headline results"},{"category":"section","location":"auto/#Reading-the-comparison-table","page":"Auto: heuristics and benchmarks","text":"For each cell the script prints Auto/best (a ratio ≥ 1) along with the per-cell winner and Auto's actual pick. Auto/best = 1.0 means Auto picked the winner; Auto/best = 1.5 means whatever Auto picked was 50% slower than the per-cell winner. Investigate any row where the ratio exceeds 1.5 — those are candidate cells for tightening one of the constants in the table above.","title":"Reading the comparison table"},{"category":"section","location":"auto/#Caching-with-SearchProperties","page":"Auto: heuristics and benchmarks","text":"Every call to searchsortedlast!(out, v, queries; strategy = Auto()) against the same v re-runs the same probes — _sampled_looks_linear(v) reads 11 elements (~25 ns), and the cost is per-call regardless of how many times you've already searched v. For callers issuing many short batches against a single sorted vector (interpolation segment lookups being the obvious case), caching the probes once and reusing the result is a real win.\n\nThe cache is a small isbits struct, SearchProperties, that Auto accepts via Auto(props):\n\nusing FindFirstFunctions\n\nv = collect(0.0:0.001:100.0)\nprops = SearchProperties(v)            # run probes once\nstrat = Auto(props)                    # `Auto` holding the cached facts\n\n# Every subsequent searchsortedlast!/searchsortedfirst! call skips the\n# linearity probe inside Auto.\nqueries = sort!(rand(8) .* 100.0)\nout = Vector{Int}(undef, length(queries))\nsearchsortedlast!(out, v, queries; strategy = strat)\n\nSearchProperties is isbits — it travels in registers and copies are free. Auto(props) is itself zero-allocation; the resulting Auto is a single concrete struct, not a parametric type, so call sites stay type-stable without specialization explosions.\n\nCurrently consumed: props.is_linear (replaces Auto's _sampled_looks_linear probe in the batched path). The other fields (has_props, has_nan) are populated by SearchProperties(v) for forward compatibility but no strategy reads them yet. Construction cost is O(1) for integer eltypes (only the sampled-linearity probe runs) and O(n) for floating-point eltypes (additionally any(isnan, v)).\n\nTrust contract: the cache is not invalidated automatically. If v mutates after SearchProperties(v), the caller must reconstruct the cache. Lying to Auto via a hand-constructed SearchProperties(true, true, false) on genuinely non-linear data is correctness-preserving (the chosen InterpolationSearch falls through to BracketGallop from a bad guess — slow but still O(log n)), so the worst case of a stale cache is a performance regression, not wrong answers.","title":"Caching with SearchProperties"},{"category":"section","location":"auto/#When-Auto-is-wrong-for-you","page":"Auto: heuristics and benchmarks","text":"If your workload sits in a corner that Auto doesn't read well, pin the strategy directly:\n\n# Sorted batch over a known-linear range, large m and n: skip the probe.\nsearchsortedlast!(out, v, queries; strategy = InterpolationSearch())\n\n# Sorted batch but queries are guaranteed adjacent: don't pay for the\n# 5 linear probes in ExpFromLeft.\nsearchsortedlast!(out, v, queries; strategy = LinearScan())\n\n# No hint and the access pattern is random: skip Auto's probes entirely.\nsearchsortedlast!(out, v, queries; strategy = BinaryBracket())\n\nThe strategy types are zero-allocation singletons, so pinning is free at runtime and just removes the heuristic from the hot path.","title":"When Auto is wrong for you"},{"category":"section","location":"#FindFirstFunctions.jl","page":"Home","text":"FindFirstFunctions.jl is a library of accelerated findfirst-style and sorted-search routines. The package provides:\n\nA strategy-dispatched sorted-search API: a single pair of Base.searchsortedfirst / Base.searchsortedlast overloads that take a SearchStrategy as the first positional argument.\nBatched in-place lookups searchsortedfirst! / searchsortedlast! that pick a strategy automatically.\nGuessers that supply per-vector hints based on linear extrapolation or a cached previous result.\nEquality search via findfirstequal and findfirstsortedequal.","title":"FindFirstFunctions.jl"},{"category":"section","location":"#Installation","page":"Home","text":"using Pkg\nPkg.add(\"FindFirstFunctions\")","title":"Installation"},{"category":"section","location":"#Guide","page":"Home","text":"Interface and extension rules — the public API surface, the contract a SearchStrategy subtype must satisfy, and how to add a new one.\nSearch strategies — catalog of the built-in strategies, when each one is fast, and when it falls back to plain binary search.\nGuessers — the Guesser type and how to plug it into the strategy dispatch via GuesserHint.\nAuto: heuristics and benchmarks — what Auto picks in every regime, the crossover constants, and the benchmark script that validates them.\nEquality search — the dedicated equality routines findfirstequal and findfirstsortedequal, which live outside the strategy framework because their return type is Union{Int, Nothing}.","title":"Guide"},{"category":"section","location":"#Quick-example","page":"Home","text":"using FindFirstFunctions\n\nv = collect(0.0:0.1:10.0)\nqueries = sort!(rand(100) .* 10)\n\n# Single query with a hint.\ni = searchsorted_last(BracketGallop(), v, 3.14, 30)\n\n# Batched, with strategy chosen by Auto.\nidx = Vector{Int}(undef, length(queries))\nsearchsortedlast!(idx, v, queries)","title":"Quick example"},{"category":"section","location":"#Contributing","page":"Home","text":"Please refer to the SciML ColPrac: Contributor's Guide on Collaborative Practices for Community Packages for guidance on PRs, issues, and other matters relating to contributing to SciML.\nSee the SciML Style Guide for common coding practices and other style decisions.\nThere are a few community forums:\nThe #diffeq-bridged and #sciml-bridged channels in the Julia Slack\nThe #diffeq-bridged and #sciml-bridged channels in the Julia Zulip\nOn the Julia Discourse forums\nSee also SciML Community page","title":"Contributing"},{"category":"section","location":"#Reproducibility","page":"Home","text":"<details><summary>The documentation of this SciML package was built using these direct dependencies,</summary>\n\nusing Pkg # hide\nPkg.status() # hide\n\n</details>\n\n<details><summary>and using this machine and Julia version.</summary>\n\nusing InteractiveUtils # hide\nversioninfo() # hide\n\n</details>\n\n<details><summary>A more complete overview of all dependencies and their versions is also provided.</summary>\n\nusing Pkg # hide\nPkg.status(; mode = PKGMODE_MANIFEST) # hide\n\n</details>\n\nusing TOML\nusing Markdown\nversion = TOML.parse(read(\"../../Project.toml\", String))[\"version\"]\nname = TOML.parse(read(\"../../Project.toml\", String))[\"name\"]\nlink_manifest = \"https://github.com/SciML/\" * name * \".jl/tree/gh-pages/v\" * version *\n                \"/assets/Manifest.toml\"\nlink_project = \"https://github.com/SciML/\" * name * \".jl/tree/gh-pages/v\" * version *\n               \"/assets/Project.toml\"\nMarkdown.parse(\"\"\"You can also download the\n[manifest]($link_manifest)\nfile and the\n[project]($link_project)\nfile.\n\"\"\")","title":"Reproducibility"},{"category":"section","location":"equality/#Equality-search","page":"Equality search","text":"This page documents the package's equality-search routines — findfirstequal and findfirstsortedequal. They answer a different question from the Search strategies covered elsewhere:\n\nSorted-search strategies answer \"where would x insert?\". The return value is always an in-range index — the bracketing position.\nEquality search answers \"does x exist, and if so at what index?\". The return value is nothing if x is not present.\n\nThese two surfaces live side-by-side because the second cannot be expressed as a SearchStrategy — its return type differs (Union{Int, Nothing} vs. in-range Int), and the unsorted variant doesn't even require a sorted input.\n\nFor a strategy-framework-compatible sorted equality search that returns Int with a sentinel (so it composes with the rest of the strategy dispatch), see findequal on the Search strategies page. findequal(BisectThenSIMD(), v, x) is the strategy entry point that internally calls into the same algorithm as findfirstsortedequal.","title":"Equality search"},{"category":"section","location":"equality/#API-reference","page":"Equality search","text":"","title":"API reference"},{"category":"section","location":"equality/#When-to-use-which","page":"Equality search","text":"Question Vector Recommended\nDoes x occur in this unsorted vector? any findfirstequal\nDoes x occur in this sorted vector? DenseVector{Int64} + Int64 findfirstsortedequal (or findequal(BisectThenSIMD(), v, x) for the sentinel-returning variant)\nDoes x occur in this sorted vector? other eltypes findequal with any strategy\nWhere would x insert into this sorted vector? any searchsorted_first(strategy, v, x[, hint])","title":"When to use which"},{"category":"section","location":"equality/#SIMD-primitives","page":"Equality search","text":"Both equality functions are backed by the same SIMD-equality LLVM IR scaffolding used internally throughout the package (load <8 x i64>, icmp eq, cttz on the bitmask of the 8-wide compare). The IR template FindFirstFunctions._simd_scan_ir (internal) generates this for the equality predicate; the same template generates the > / >= variants for SIMDLinearScan.\n\nThe SIMD path is Int64-only — the LLVM IR is keyed off i64 element width and 8-byte stride. Every other element type and storage layout falls through to a scalar findfirst(==(x), v) path. Specifically:\n\nfindfirstequal(x::Int64, v::DenseVector{Int64}) — full SIMD scan.\nfindfirstequal(x, v) (generic) — findfirst(isequal(x), v).\nfindfirstsortedequal(x::Int64, v::DenseVector{Int64}) — branchless binary bisection to a small basecase, then SIMD equality scan within the basecase window. The bisection uses a strict < predicate so that earlier duplicates are not skipped (a previous version of the routine used <= and could return a later duplicate; fixed in 2.0).","title":"SIMD primitives"},{"category":"section","location":"equality/#Sentinel-vs.-Nothing","page":"Equality search","text":"The two equality APIs differ in how they report \"not found\":\n\nfindfirstequal(x, v)        # -> Union{Int, Nothing}, `nothing` on miss\nfindfirstsortedequal(x, v)  # -> Union{Int, Nothing}, `nothing` on miss\nfindequal(strategy, v, x)   # -> Int, firstindex(v) - 1 on miss\n\nfindequal's sentinel is type-stable and composes with the rest of the strategy dispatch — that's the recommended choice for new sorted-equality code. The two findfirst*equal names continue to return Union{Int, Nothing} for backwards compatibility with callers and pattern matching against nothing.","title":"Sentinel vs. Nothing"},{"category":"function","location":"equality/#FindFirstFunctions.findfirstequal","page":"Equality search","text":"findfirstequal(x, A) -> Union{Int, Nothing}\n\nFind the first index in A where the value equals x. Returns nothing if x does not occur in A.\n\nThis function does not assume A is sorted. For sorted vectors, see findfirstsortedequal (a bisect-then-SIMD specialization on DenseVector{Int64}) or findequal (the strategy-framework equality wrapper that returns an Int with a sentinel).\n\nOn 64-bit platforms the (x::Int64, A::DenseVector{Int64}) method uses a custom LLVM IR SIMD scan (load 8 lanes, icmp eq, cttz on the mask) — about 8× faster than the scalar findfirst(==(x), v) on modern x86-64; on 32-bit platforms it uses a scalar loop. Every other element-type and array-storage combination falls back to findfirst(isequal(x), A).\n\n\n\n\n\n","title":"FindFirstFunctions.findfirstequal"},{"category":"function","location":"equality/#FindFirstFunctions.findfirstsortedequal","page":"Equality search","text":"findfirstsortedequal(var::Int64, vars::DenseVector{Int64}) -> Union{Int64, Nothing}\n\nFind the index of the first occurrence of var in the sorted vector vars. Returns nothing if var does not occur. Specialized for DenseVector{Int64} via a branchless binary bisection down to a small basecase, followed by the same SIMD equality scan that backs findfirstequal — faster than plain findfirst(==(var), vars) or searchsortedfirst + post-check for typical Int64 vectors. Every other element-type and array-storage combination (including native Int on 32-bit platforms, where Int is not Int64) falls back to a generic searchsortedfirst + equality post-check.\n\nThe strategy-framework equivalent is findequal(BisectThenSIMD(), vars, var); that wrapper returns an Int with a sentinel (firstindex(v) - 1) for \"not found\", which is type-stable and composes with the rest of the strategy dispatch. Prefer findequal for new code; findfirstsortedequal remains as the dedicated Union{Int64, Nothing}-returning name.\n\n\n\n\n\n","title":"FindFirstFunctions.findfirstsortedequal"}]
}
